def insert(x):
    i = 0
    # repeat
        j = h(x)
        if (table[j] = null or table[slot] = DELETE): # 충돌이 일어나지 않았을 경우
            table[j] = x
            return j
        else:
            j += 1
    # until(i=m)
    
